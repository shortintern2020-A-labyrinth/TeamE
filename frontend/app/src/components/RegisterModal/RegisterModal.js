import React from "react";
import styles from "./RegisterModal.module.css";

const RegisterModal = (props) => {
  return (
    <div className={styles.screen}>
      <div className={styles.container}>RegisterModal</div>
    </div>
  );
};

export default RegisterModal;
